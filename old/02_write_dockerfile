# Each dockerfile is based off of a pre-existing image
FROM ubuntu:latest

# Nothing comes installed on the barebones ubuntu image, so you have to install
# the packages that you want yourself
RUN apt-get update && \
    apt-get upgrade -y
RUN apt-get install -y vim curl

# We can set the default directory that things run from and copy our source into
# the image
WORKDIR /app
ADD resources/2_some.txt /app

# Upon starting, will run "/bin/sh echo hello world!"
CMD "echo hello world!"
