docker run --name ES -d elasticsearch

# --name: we're giving the container a specific name, otherwise docker-engine
#         will just generate a random name for the container
#
# -d: detach/run in background
